split multipart stl

see if I can combine stltool.py and the stl class inside of stlviewer - get rid of duplicated code

need to present a message box when the plate is full - currently printing a message